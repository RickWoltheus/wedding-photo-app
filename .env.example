# --- Option A: OAuth (recommended – uploads to your My Drive, no Shared Drive) ---
# 1. Google Cloud Console → APIs & Services → Credentials → Create OAuth 2.0 Client ID → Web application
# 2. Add redirect URI: http://localhost:3000/api/drive-auth/callback (or your deployed origin + /api/drive-auth/callback)
# 3. Copy Client ID and Client secret here:
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# 4. Visit http://localhost:3000/api/drive-auth once (sign in with your Google account), then paste the refresh token shown:
DRIVE_REFRESH_TOKEN=

# --- Option B: Service account (uploads only to Shared Drive folder) ---
# DRIVE_KEY=  # base64-encoded service account JSON

# --- Both options: folder to upload into ---
# Folder ID from the folder URL in Drive (.../folders/THIS_ID). For OAuth use any folder in your My Drive.
DRIVE_FOLDER_ID=

# --- Access gate (QR flow) ---
# Secret code that gets encoded in the QR; guests open app with ?code=this and get a cookie.
APP_ACCESS_CODE=
# Full app URL for the QR link (e.g. https://photos.jouwdag.nl). No trailing slash.
NEXT_PUBLIC_APP_URL=

# --- QR admin page (/qr) — password to view and download the QR ---
QR_PASSWORD=
